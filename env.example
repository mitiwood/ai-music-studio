# Kenny's Music Studio - 환경변수 설정
# 
# 이 파일을 .env.local 로 복사한 후 실제 값을 입력하세요
# cp .env.example .env.local
#
# .env.local 은 .gitignore에 포함되어 git에 커밋되지 않습니다
# Vercel 배포 시 Dashboard → Settings → Environment Variables 에서 직접 입력

# kie.ai API 키 (https://kie.ai 에서 발급)
KIE_API_KEY=your_kie_api_key_here
